'use client'

import React from 'react'
import { data } from '@/public/data'

// const properties = data.map(house =>(
//   <div key={house.id} className='h-[200px] w-[212px] bg-orange-500'>
//     <Image src={house.image} />

//   </div>
// )
// )

export default function Page() {
  return;
}
